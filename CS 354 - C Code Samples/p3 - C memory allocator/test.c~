#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[])
{
int size = 9;

int nsize = size + (4 - (size % 4)) * (0 < size % 4);
fprintf(stdout, "%i ", nsize);
return 0;
}
